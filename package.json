{
  "name": "graph-art-setup",
  "version": "0.0.1",
  "description": "Manages the client and server application",
  "main": "index.js",
  "scripts": {
    "install": "(npm install) && (cd client && npm install) && (cd server && npm install)",
    "develop": "concurrently \"cd client && PORT=3000 npm start\" \"cd server && PORT=3001 npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fitfab/graph-art-setup.git"
  },
  "keywords": [
    "nodejs",
    "express",
    "reactjs",
    "and",
    "graphql",
    "apollo-graphql"
  ],
  "author": "Miguel Julio <mjulio@artnet.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fitfab/graph-art-setup/issues"
  },
  "homepage": "https://github.com/fitfab/graph-art-setup#readme",
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
